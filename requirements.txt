# Pinned with upper bounds on purpose. This is a research repo: a silent
# dependency major-version bump that changes dtype or rounding behaviour would
# invalidate results without failing a test. Bump deliberately, then re-run
# `pytest tests -q` INCLUDING the network contract tests before trusting output.
#
# Verified 2026-09-02 on python 3.11 / pandas 3.0.2 / numpy 2.4.4: every upstream
# pickle in production/ reads cleanly with correct dtypes and zero nulls.
# pandas 3 is fine for this repo. See docs/FINDINGS.md.

pandas>=2.2,<4
numpy>=1.26,<3
pyarrow>=15,<27
duckdb>=1.0,<2
pyyaml>=6.0,<7
python-dotenv>=1.0,<2

scipy>=1.11,<2
statsmodels>=0.14,<1

matplotlib>=3.8,<4
jinja2>=3.1,<4

pytest>=8.0,<9
# Writes data/results/pytest_report.json, which the build monitor reads to
# derive contract-gate status. Without it every gate reads not_built and the
# top-line gate is pinned amber.
pytest-json-report>=1.5,<2

# Price providers - install the ones you actually use (Phase 2).
robin-stocks>=3.0,<4
alpaca-py>=0.20,<1
yfinance>=0.2.40,<1
