# Order: inventory the docs directory

```json
{ "id": "004-count-docs", "from": "andrew", "title": "inventory the docs directory" }
```

Count how many `.md` files are under `docs/` in this repository, and list the
five with the most lines, largest first, with their line counts.

Write the answer to `ORDER_RESULT.md` at the repository root. Keep it short.
