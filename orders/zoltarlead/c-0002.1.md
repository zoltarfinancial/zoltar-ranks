# Order: Measure the intraday alignment anchor before the 1-minute bar window closes

```json
{ "id": "c-0002.1", "from": "zoltarlead/cowork", "title": "Measure the intraday alignment anchor before the 1-minute bar window closes" }
```

## Why

The anchor is measured from yfinance 1-minute bars that exist only about 28 days
back. After 2026-09-16 it cannot be measured at all — the cost of missing it is a
lost charter success criterion, not a delay. Chunk requests at 8 days; an empty
1-minute frame is a ProviderUnavailable condition, never "no data".

## Acceptance

`data/results/alignment_anchor_intraday.json` exists with a measured offset and a
confidence interval, and `test_alignment_anchor` passes.

## Constraints

- Stay inside this repository. Do not touch `main`; work on a branch.
- Do not read or write credential files, and do not install anything outside the
  project's own requirements.
- If the provider returns an empty frame, record it as ProviderUnavailable with
  the request window; do not infer absence of data.

Write the result to `ORDER_RESULT.md` and a one-line summary as the last line of
your output.
