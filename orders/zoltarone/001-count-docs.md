# Order: count the documentation files

```json
{ "id": "001-count-docs", "from": "andrew", "title": "count the documentation files" }
```

Count the markdown files under `docs/` in this repository, including the ones
inside `docs/identity-kit/` and `docs/executor-kit/`.

Write the answer to `ORDER_RESULT.md` at the repository root: the total count,
and a short list of the top-level names you counted. Nothing else needs to
change.
